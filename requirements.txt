# Runtime dependencies for the Shadow Mode LLM Evaluator.
# Pin to compatible minor ranges; regenerate a lockfile for deploys if desired.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
httpx>=0.27,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0
aiosqlite>=0.20,<1.0
